# Pinned to versions that work together with Qwen3-TTS as of 2026-05.
# qwen-tts brings transformers+torch+accelerate+librosa transitively.
# torch 2.2.2 (what qwen-tts pins) is built against numpy <2 — that's
# why numpy is pinned below; pulling 2.x at runtime crashes the
# C-extensions inside torch.

qwen-tts==0.1.1
numpy<2

# Server surface — fastapi + uvicorn + loguru for logs.
fastapi>=0.115
uvicorn[standard]>=0.32
loguru>=0.7

# Optional but recommended on the 4090 host:
# flash-attn>=2.5  # massive speed-up for the transformer pass.
